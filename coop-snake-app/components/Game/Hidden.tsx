// TODO:
// Added as a hack to pass debug metadata as an entity.
// Should be replace with something like 'zustand' in the future.

import { PureComponent } from "react";

export class Hidden extends PureComponent {
  render() {
    return <></>;
  }
}
